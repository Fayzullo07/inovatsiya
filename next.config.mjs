/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "cdn.devdojo.com",
      "cruip-tutorials.vercel.app",
    ],
  },
};

export default nextConfig;
